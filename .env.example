# Postgres connection used by the app and the test suite.
DATABASE_URL=postgres://postgres:postgres@localhost:5433/incident_copilot

# Anthropic API key. Required from M6 onward for the agent and judge.
# Not needed for M0 to M5; leave blank until then.
ANTHROPIC_API_KEY=
