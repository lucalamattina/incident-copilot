/**
 * Pinned model ids. The exact version (not just the family) is part of what
 * makes an evaluation pass-rate meaningful, so it lives in one place and an
 * upgrade is a one-line, recorded change.
 */
export const AGENT_MODEL = "claude-sonnet-4-6";
export const JUDGE_MODEL = "claude-opus-4-8";
